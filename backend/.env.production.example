# Copier sur le VPS :
#   cp backend/.env.production.example backend/.env
#   nano backend/.env
# Puis : docker compose up --build -d

SECRET_KEY=remplacez-par-une-cle-secrete-tres-longue
FRONTEND_URL=http://145.223.121.38:5181

# --- Hostinger : hPanel → Emails → creer noreply@votredomaine.com ---
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=noreply@votredomaine.com
SMTP_PASSWORD=le_mot_de_passe_de_cette_boite_email
SMTP_FROM=Conseil Regional Ouest <noreply@votredomaine.com>
SMTP_USE_SSL=false

# OBLIGATOIRE pour envoyer aux clients :
EMAIL_DEV_MODE=false

UPLOAD_DIR=./uploads
