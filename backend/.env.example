# Copier en .env sur le VPS : cp .env.example .env

SECRET_KEY=changez-moi-avec-une-cle-secrete-longue
DATABASE_URL=postgresql://cro:cro_secret_2024@localhost:5434/stock_cro

# --- Email (Hostinger ou Gmail) ---
# Tant que EMAIL_DEV_MODE=true OU SMTP_USER est vide, aucun email n'est envoye :
# le code s'affiche dans les logs Docker (docker logs cro_stock_api).

# Hostinger (email professionnel du domaine) :
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=Conseil Regional Ouest <noreply@votredomaine.com>

# Mettre false pour envoyer les vrais emails une fois SMTP rempli :
EMAIL_DEV_MODE=true

FRONTEND_URL=http://145.223.121.38:5181
UPLOAD_DIR=./uploads
